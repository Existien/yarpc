[project]
name = "python_mocks"
version = "0.1.0"
classifiers = ["Private :: Do not Upload"]
description = "Python D-Bus mocks"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "", email = "dummy@mail.com"},
]
requires-python = ">=3.12"
dependencies = [
    "dbus-next>=0.2.3",
]

[project.scripts]
mock = "python_mocks.main:main"

[tool.uv]
package = true
