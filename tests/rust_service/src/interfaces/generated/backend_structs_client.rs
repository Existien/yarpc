// Generated by YARPC
// Version:  0.1.0
// Definition:
//   File: /workspace/tests/definitions/03_structs.yml
//   Object: Structs
//   Template: rust/client.j2

#![allow(dead_code)]

use std::sync::Arc;
use dbus::nonblock::{SyncConnection, MsgMatch, Proxy};
use dbus::message::{Message, MatchRule};
use dbus::arg::{PropMap, ReadAll, RefArg, Variant};
use super::connection::{connect, close};
use super::simple_struct::SimpleStruct;
use super::item::Item;

#[derive(Default)]
#[derive(Clone)]
pub struct BackendStructsClientProperties {
    pub simple: Option<SimpleStruct>,
}

impl From<BackendStructsClientProperties> for PropMap {
    fn from(props: BackendStructsClientProperties) -> PropMap {
        let mut prop_map = PropMap::new();
        if props.simple.is_some() {
            prop_map.insert("Simple".to_string(), Variant(Box::new(props.simple.unwrap())));
        }
        prop_map
    }
}

impl From<PropMap> for BackendStructsClientProperties {
    fn from(prop_map: PropMap) -> Self {
        BackendStructsClientProperties {
            simple: prop_map.get("Simple").map(|v|{SimpleStruct}).flatten(),
        }
    }
}

/**
   D-Bus client for the com.yarpc.backend.structs D-Bus interface
 */
pub struct BackendStructsClient {
    connection: Option<Arc<SyncConnection>>,
    timeout: std::time::Duration,
    signal_handlers: Vec<MsgMatch>,
}

impl BackendStructsClient {

    /**
       Configures the D-Bus connection.
       Sets connection timeout to 5s.

       Timeout determines how long a connection attempt
       during a method call may take before panicking.
     */
    pub async fn connect() -> Result<Self, dbus::Error> {
        BackendStructsClient::connect_timeout(std::time::Duration::from_secs(5)).await
    }

    /**
       Configures the D-Bus connection.

       Timeout determines how long a connection attempt
       during a method call may take before panicking.
     */
    pub async fn connect_timeout(timeout: std::time::Duration) -> Result<Self, dbus::Error> {
        let (connection, _) = connect().await?;
        Ok(Self{connection: Some(connection), timeout, signal_handlers: Vec::new()})
    }


    /**
       Disconnects client from D-Bus
     */
    pub async fn close(&mut self) -> Result<(), dbus::Error> {
        if self.connection.is_some() {
           for s in &self.signal_handlers {
                self.connection.as_ref().unwrap().remove_match(s.token()).await?;
            }
        }
        self.connection = None;
        let _ = close().await;
        Ok(())
    }

    pub async fn remove_signal_handler(&mut self, token: dbus::channel::Token)-> Result<(), dbus::Error> {
        let idx = self.signal_handlers.iter().position(|x| x.token() == token);
        match (idx, &self.connection) {
            (Some(i), Some(c)) => {
                self.signal_handlers.remove(i);
                c.remove_match(token).await
            }
            _ => Err(dbus::Error::new_failed("Client not connected to D-Bus or token invalid")),
        }
    }


    pub async fn get_all_properties(&self) -> Result<(BackendStructsClientProperties,), dbus::MethodErr> {
        match &self.connection {
            Some(c) => {
                let proxy = Proxy::new("com.yarpc.backend", "/com/yarpc/backend/structs", self.timeout, c.clone());
                match proxy.method_call::<(PropMap,), (String,), _, _>("org.freedesktop.DBus.Properties", "GetAll", ("com.yarpc.backend.structs".to_string(),)).await {
                    Ok((r,)) => {
                        let props = BackendStructsClientProperties {
                            simple: r.get("Simple").map(|v|{SimpleStruct}).flatten(),
                        };
                        Ok((props,))
                    },
                    Err(e) => Err(dbus::MethodErr::from(e))
                }
            },
            None => Err(dbus::MethodErr::failed("Client not connected to D-Bus")),
        }
    }

    pub async fn on_properties_changed<F: FnMut(Message, BackendStructsClientProperties) -> bool + Send + 'static>(&mut self, mut f: F) -> Result<dbus::channel::Token, dbus::Error> {
        match &self.connection {
            Some(c) => {
                let mr = MatchRule::new_signal("org.freedesktop.DBus.Properties".to_string(), "PropertiesChanged".to_string())
                .with_path("/com/yarpc/backend/structs")
                .with_sender("com.yarpc.backend");
                let cb = move |msg, (member, p, _): (String,PropMap,Vec<String>)|{
                        if member == "com.yarpc.backend.structs" {
                            f(msg, BackendStructsClientProperties {
                                simple: p.get("Simple").map(|v|{SimpleStruct}).flatten(),
                            })
                        } else {
                            false
                        }
                };
                let signal_matcher = c.add_match(mr).await?.cb(cb);
                let token = signal_matcher.token();
                self.signal_handlers.push(signal_matcher);
                Ok(token)
            },
            None => Err(dbus::Error::new_failed("Client not connected to D-Bus")),
        }
    }


    /**
        a method with a struct as args

        Args:
            simpleStruct: the SimpleStruct to send

        Returns:
            the SimpleStruct
     */
    pub async fn send_struct(&self, simple_struct: SimpleStruct, ) -> Result<(SimpleStruct,), dbus::MethodErr> {
        match &self.connection {
            Some(c) => {
                let proxy = Proxy::new("com.yarpc.backend", "/com/yarpc/backend/structs", self.timeout, c.clone());
                match proxy.method_call::<(SimpleStruct,), (SimpleStruct, ), _, _>("com.yarpc.backend.structs", "SendStruct", (simple_struct, )).await {
                    Ok(r) => Ok(r),
                    Err(e) => Err(dbus::MethodErr::from(e))
                }
            },
            None => Err(dbus::MethodErr::failed("Client not connected to D-Bus")),
        }
    }
    /**
        Set handler for StructReceived signal

        Args:
            handler: the signal handler
     */
    pub async fn on_struct_received<R: ReadAll, F: FnMut(Message, R) -> bool + Send + 'static>(&mut self, f: F) -> Result<dbus::channel::Token, dbus::Error> {
        match &self.connection {
            Some(c) => {
                let mr = MatchRule::new_signal("com.yarpc.backend.structs", "StructReceived")
                .with_path("/com/yarpc/backend/structs")
                .with_sender("com.yarpc.backend");
                let signal_matcher = c.add_match(mr).await?.cb(f);
                let token = signal_matcher.token();
                self.signal_handlers.push(signal_matcher);
                Ok(token)
            },
            None => Err(dbus::Error::new_failed("Client not connected to D-Bus")),
        }
    }
    pub async fn get_simple(&self) -> Result<Option<SimpleStruct>, dbus::MethodErr> {
        match &self.connection {
            Some(c) => {
                let proxy = Proxy::new("com.yarpc.backend", "/com/yarpc/backend/structs", self.timeout, c.clone());
                match proxy.method_call::<(Variant<SimpleStruct>,), (String,String,), _, _>("org.freedesktop.DBus.Properties", "Get", ("com.yarpc.backend.structs".to_string(), "Simple".to_string())).await {
                    Ok((r,)) => {
                        Ok(SimpleStruct)
                    },
                    Err(e) => Err(dbus::MethodErr::from(e))
                }
            },
            None => Err(dbus::MethodErr::failed("Client not connected to D-Bus")),
        }
    }

    pub async fn set_simple(&self, value: SimpleStruct) -> Result<(), dbus::MethodErr> {
        match &self.connection {
            Some(c) => {
                let proxy = Proxy::new("com.yarpc.backend", "/com/yarpc/backend/structs", self.timeout, c.clone());
                match proxy.method_call::<(), (String,String,Variant<SimpleStruct>,), _, _>("org.freedesktop.DBus.Properties", "Set", ("com.yarpc.backend.structs".to_string(), "Simple".to_string(), Variant(value))).await {
                    Ok(()) => Ok(()),
                    Err(e) => Err(dbus::MethodErr::from(e))
                }
            },
            None => Err(dbus::MethodErr::failed("Client not connected to D-Bus")),
        }
    }

}