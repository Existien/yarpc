[project]
name = "test-library"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psutil==7.2.1",
    "python-mocks",
    "robotframework==7.4.1",
]

[tool.uv.sources]
python-mocks = { path = "../../python_mocks" }

[tool.uv]
package = true
