/* Generated by YARPC
 * Version:  0.1.0
 * Definition:
 *   File: /workspace/tests/definitions/01_minimal.yml
 *   Object: Minimal
 *   Template: qt6/service_header.j2
 */
#pragma once
#include <QObject>
#include <qqmlintegration.h>
#include <QDBusAbstractAdaptor>
#include <QDBusConnection>
#include <QDBusMessage>
#include <memory>
#include "DBusError.hpp"
namespace gen::minimal {

/**
 * @brief D-Bus adaptor for the Minimal interface.
 */
class MinimalInterfaceAdaptor : public QDBusAbstractAdaptor {
    Q_OBJECT
    Q_CLASSINFO("D-Bus Interface", "com.yarpc.testservice.minimal")
public:
    MinimalInterfaceAdaptor(QObject* parent = nullptr);
public slots:
    /**
     * @brief a simple method without args
     */
    void Bump(const QDBusMessage &message);
signals:
    /**
     * @brief a simple signal without arguments
     */
    void Bumped();
};

class BumpArgs {
    Q_GADGET
};

class BumpPendingReply : public QObject {
    Q_OBJECT
    QML_UNCREATABLE("")
    QML_ELEMENT
public:
    BumpPendingReply(QDBusMessage call, QObject *parent);
public slots:
    BumpArgs* args();
    void sendReply();
    void sendError(const QString &name, const QString &message);
    void sendError(const DBusError &error);
private:
    QDBusMessage m_call;
    BumpArgs m_args;
};


class MinimalInterface : public QObject {
    Q_OBJECT
    QML_ELEMENT
    Q_PROPERTY(bool connected READ getConnected NOTIFY connectedChanged)
public:
    MinimalInterface(QObject* parent = nullptr);
    bool getConnected() const;
    void callFinished(const QDBusMessage &reply);
    void handleBumpCalled(QDBusMessage call);

public slots:
    void connect();
    void disconnect();

    void EmitBumped();

signals:
    void connectedChanged();
    void bumpCalled(BumpPendingReply* reply);

private:
    MinimalInterfaceAdaptor *m_adaptor;
    std::unique_ptr<QDBusConnection> m_connection = nullptr;
};

}