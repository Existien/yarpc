/* Generated by YARPC
 * Version:  0.1.0
 * Definition:
 *   File: /workspace/tests/definitions/qt6/01_minimal.yml
 *   Template: qt6/object_path_header.j2
 */
#pragma once
#include <QObject>
#include <QQmlEngine>
#include <qqmlintegration.h>
#include <QDBusConnection>
#include <QDBusMessage>
#include <memory>
#include "MinimalInterfaceAdaptor.hpp"
namespace gen::minimal {

/**
 * @brief Container for D-Bus interfaces exposed under the object path "/com/yarpc/testservice/minimal"
 */
class MinimalTestserviceYarpcComObjectPath : public QObject {
    Q_OBJECT
public:
    MinimalTestserviceYarpcComObjectPath(QObject* parent = nullptr);

    /**
     * @brief Returns whether there are any interfaces registered under this object path.
     *
     * @returns whether there are any interfaces registered
     */
    bool hasRegistrations() const;

    /** @brief Pointer to the adaptor for a registered MinimalInterface interface or null if it isn't registered. */
    MinimalInterfaceAdaptor *m_minimal = nullptr;

};

/**
 * @brief D-Bus connection singleton.
 *
 * Responsible for managing the registered interfaces
 */
class Connection : public QObject {
Q_OBJECT
/** @brief Whether there are any interfaces registered. */
Q_PROPERTY(bool connected READ getConnected NOTIFY connectedChanged)
public:

    /**
     * @brief Returns the singleton instance.
     *
     * @returns the singleton instance
     */
    static Connection& instance();

    /**
     * @brief Disconnects from the D-Bus if no interfaces are registered.
     */
    void disconnectIfUnused();

    /**
     * @brief Returns whether the a connection to the D-Bus was established.
     *
     * @returns whether a connection was established
     */
    bool getConnected() const;

    /**
     * @brief Send a D-Bus message.
     *
     * @param message the message to send
     */
    void send(const QDBusMessage &message);

    /**
     * @brief Registers the Minimal interface under its object path.
     *
     * @param interface pointer to the Minimal object to register
     */
    void registerMinimal(QObject* interface);

    /**
     * @brief Unregisters the Minimal.
     */
    void unregisterMinimal();

    /**
     * @brief Returns whether the Minimal interface is registered.
     *
     * @returns whether the interface is registered
     */
    bool isMinimalRegistered() const;
    MinimalInterfaceAdaptor * Minimal();

public slots:
    /**
     * @brief Establishes a connection to the D-Bus.
     */
    void connect();

    /**
     * @brief Severs a connection to the D-Bus.
     */
    void disconnect();
signals:
    /**
     * @brief Emitted when the connection status changes.
     */
    void connectedChanged();

    /**
     * @brief Emitted when interfaces are registered or unregistered.
     */
    void registrationChanged();
private:
    Connection();

private:
    void updateRegistrations();
    std::unique_ptr<QDBusConnection> m_connection = nullptr;

    std::unique_ptr<MinimalTestserviceYarpcComObjectPath> m_MinimalTestserviceYarpcComObjectPath = nullptr;

    QObject* m_Minimal = nullptr;

};

}